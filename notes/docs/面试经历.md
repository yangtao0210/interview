### 百度

```java
一面凉经
1.自我介绍 + 项目介绍;
2.RabbitMQ的延迟队列 & 底层实现;
3.一些其他的中间件产品,比如kafka,RocketMQ之类的;
4.对Redis的了解多少？它的过期时间是怎么设置的;
5.Mysql索引的底层,聚簇 & 非聚簇索引的区别;
6.Mysql的 Innodb 引擎 (聊了缓存池,更新时，缓存池的数据什么时候刷新到内存中（聊了Redo的刷新策略));
7.聊一下Mysql的事务（聊了事务特性ACID特性,并发事务 & 事务隔离级别）;
8.TCP的滑动窗口;
9.TCP的三次握手 & 为什么需要三次握手?
10.线程的创建方式;
11.结合实际应用说一下接口和抽象类;
10.算法怎么样?
11.看你简历上的期望城市是***?
12.最近有在看什么书?答：最近在找工作，在看一些面经。问：面经有帮忙吗？查漏补缺，深入学习。
13.反问：问了一下面试评价！有什么建议？等后续通知 
```

### SHEIN

```java
一面问题(时长：35分钟左右)
1.简单自我介绍
2.Java线程池有几种类型，简单介绍;
3.AQS有了解吗，从ReentrantLock的lock实现来介绍一下;
4.聊一下Synchronized关键字,是哪个版本开始优化的？（1.6 还是1.8）,做了哪些优化?
5.上面提到了重量级锁,那什么是重量级锁呢?-----扯了锁升级的过程;
6.插了一句：偏向锁是底层怎么实现,聊一下吧---CAS操作;
7.springboot只是简单使用,没有了解过底层实现是吧,那就跳过;
8.聊一下MYSQL的Innodb引擎的底层;
9.Mysql索引的底层 & 聚簇和非聚簇索引的区别？
10.为什么索引底层用B+ 树,不用B树呢?
11.Mysql的死锁有了解吗？（怎么形成 & 怎么避免）----未答出
12.SQL调优的过程是怎样的?---explain & 里面的字段;
13.TCP和UDP有哪些方面的区别?
14.TCP的连接过程是怎样的,你说三次,为什么不能是两次/四次呢?
15.TCP在数据传输过程中是否存在溢出问题?-----------未答出
16.聊了一下他们部门的相关情况
17.Redis既然有了主从复制,为什么还需要集群模式（哨兵模式）呢？---未答出
18.大概就是这么多,反问。（随便聊了一下）
```

### 用友

```Java
一面（50分钟）
1.自我介绍;
2.简单介绍一下项目，项目的性质 、主要负责哪个部分（商品 和 库存）;
项目问题：
    批量发布怎么处理的?（MQ异步处理）
	批量发布商品时,有没有数据量的限制（接口可能有一个访问量的限制，系统中没有明文规定）;
	部署了多少MQ服务器/站点(单机的，聊了一下单机模式存在的一些缺点)
3.MQ相关问题
     3.1 用的哪个MQ--RabbitMQ
     3.2 RabbitMQ 与 其他几个消息中间件区别（从延时，并发量方面） 
     3.3 有没有了解过MQ如何处理大量消息堆积问题？如何保证消息不被重复消费？
     3.4 MQ的死信队列是什么？聊一下
4.Redis相关问题
     4.1 Redis项目中有没有用到（没有，自己看过一点）
     4.2 有哪些数据结构（ 5+3）
     4.3 主要的应用场景（做缓存，高并发场景下缓解服务器压力）
5.JVM 相关问题
      5.1 说一下JVM的内存模型(按内存私有，共享分别说明了 & 自己说了一下每个区域存储的内容)
      5.2 JVM 是怎么分代的，主要有哪些？(按照对象的年龄来分，默认15会被移动到老年代，聊了一下Minor GC & FullGC)
      5.3 JVM 的 GC 算法了解吗？（4个）
      5.4 JVM 的 GC 收集器主要有哪些？（7个）
      5.5 JVM 调优有没有了解过，说几个调优参数
6.Java并发相关
      6.1 Synchronized关键字的底层是怎么实现的？（分方法和代码块说的）
      6.2 volatile 关键字有了解吗？是如何保证可见性和有序性的？
7.算法和数据结构
      7.1 怎么学习算法的（一般都是通过刷一些算法题）,刷什么题（力扣）,怎么刷的（跟着一个开源项目，分模块来刷的）
      7.2 哪些数据结构比较熟悉(JAVA中常用的就是map);
      7.3 那聊一下HashMap的底层实现（以 1.8 来说,说了底层实现+这样做的一些好处);
      7.4 HashMap的扩容机制（是怎么扩容的，从扩容因子切入说明了一下）;
      7.5 哈希表是如何处理冲突的（说了二次哈希和链地址，因为底层结构，所以 1.8 是通过链地址来解决的）;
      7.6 HashMap线程安全不？怎么保证安全（用哪个数据结构）
8.MYSQL相关问题
      8.1 MYSQL的语句的执行过程;
      8.2 MYSQL在数据成功后，写磁盘之前突然宕机了，怎么恢复数据呢（Innodb的日志先行机制 redo日志）
      8.3 redo 日志是Innodb特有的吗？其他引擎有没有
      8.4 索引的底层实现（从聚簇索引和非聚簇索引两方面说了一下），聚簇索引时Innodb特有的吗？
      8.5 MYSQL的优化（索引优化 & SQL优化）？怎么分析的
8.反问

二面等通知...
```

### 绿盟、浩鲸科技 08.24

```

```

